# Chaincode-Fish Marriage Phase 2 — A/B Results

Spec: data/chaincode_fish_marriage_spec.md (2026-03-25)

Generated by tests/marriage_phase2_ab_harness.py

## A/B: synthetic corpus with vs without chain_seq

Corpus: 10 synthetic 'sessions' × 20 crystals each = 200 crystals total.
Each session has internal vocabulary signature; cross-session pairs are
chain-distant (distance >= 20 between any two sessions).

**Gamma distribution (sampled 2000 pairs, no chain metadata):**

  p60=0.111, p75=0.250, p90=0.429

  threshold used: min_gamma = 0.300

| metric | without_chain | with_chain | delta |
|---|---:|---:|---:|
| edges | 2005 | 2007 | +2 |
| components_total | 5 | 5 | +0 |
| nontrivial_components | 5 | 5 | +0 |
| largest_component | 80 | 80 | +0 |

Size distributions (top 10 component sizes):

  without_chain: [80, 60, 20, 20, 20]
  with_chain:    [80, 60, 20, 20, 20]

**Interpretation.** Phase 2 temporal rescue is *additive* — it can only
ADD edges, never remove them. So `edges with_chain >= edges without_chain`
must hold. Likewise component count can only stay the same or shrink as
rescued edges merge previously-disconnected components.

## Stress: staleness filter blocks sensor-data mega-coupling

100 synthetic sensor readings, all chain-adjacent (chain_seq 0..99),
each with a single tiny dimension spike at a different position.
Pairwise gamma is below SEMANTIC_FLOOR (0.2). Without the staleness
gate, pure chain proximity would couple every pair into one giant
formation. With the gate, the temporal term is zeroed and gamma alone
(below threshold) keeps them apart.

  n_crystals: 100
  avg_gamma_sample: 0.0
  edges: 0
  largest_component: 1
  components_count: 100

**Pass criterion.** `largest_component` should remain near 1 (every
sensor isolated) — not anywhere near `n_crystals`. If we see 100, the
staleness filter is broken.

## Verdict

- A/B additivity: PASS (2007 >= 2005 edges)
- Staleness filter: PASS (largest sensor component = 1, well below 100)
